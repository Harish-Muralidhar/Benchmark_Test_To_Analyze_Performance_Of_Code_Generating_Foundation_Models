"""

"""

import random

def get_random_array(n):
    return [random.randint(1, n) for _ in range(n)]

def get_random_index(n):
    return random.randint(1, n)

def get_random_index_range(n):
    return random.randint(1, n), random.randint(1, n)

def get_random_index_range_with_lower_bound(n, lower_bound):
    return random.randint(lower_bound, n), random.randint(lower_bound, n)

def get_random_index_range_with_upper_bound(n, upper_bound):
    return random.randint(1, upper_bound), random.randint(1, upper_bound)

def get_random_index_range_with_both_bounds(n, lower_bound, upper_bound):
    return random.randint(lower_bound, upper_bound), random.randint(lower_bound, upper_bound)

def get_random_index_range_with_lower_bound_and_upper_bound(n, lower_bound, upper_bound):
    return random.randint(lower_bound, n), random.randint(lower_bound, upper_bound)

def get_random_index_range_with_lower_bound_and_upper_bound(n, lower_bound, upper_bound):
    return random.randint(lower_bound, n), random.randint(lower_bound, upper_bound)

def get_random_index_range_with_lower_bound_and_upper_bound(n, lower_bound, upper_bound):
    return random.randint(lower_bound, n), random.randint(lower_bound, upper_bound)

def get_random_index_range_with_lower_bound_and_upper_bound(n, lower_bound, upper_bound):
    return random.randint(lower_bound, n), random.randint(lower_bound, upper_bound)

def get_random_index_range_with